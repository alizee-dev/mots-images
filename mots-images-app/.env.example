# Base URL of the mots-images-api backend this frontend talks to.
# Copy this file to .env (or .env.local) and adjust per environment —
# .env itself is gitignored and should never be committed.
VITE_API_URL=http://localhost:3000
